export const signup = (req, res) => {
  res.json({
    message: "signup routes",
  });
};

export const login = (req, res) => {
  res.json({
    message: "Login routes",
  });
};

export const logout = (req, res) => {
  res.json({
    message: "Logout routes",
  });
};
